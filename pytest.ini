[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
markers =
    db: needs the SQLite test database fixture
    net: touches the network (deselected by default; run with -m net)
filterwarnings =
    ignore::DeprecationWarning
